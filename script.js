const body=document.body;const menu=document.querySelector('.menu');const nav=document.querySelector('.nav');if(menu){menu.addEventListener('click',()=>{const open=body.classList.toggle('open');menu.setAttribute('aria-expanded',String(open));});nav?.addEventListener('click',e=>{if(e.target.tagName==='A'){body.classList.remove('open');menu.setAttribute('aria-expanded','false');}})}const progress=document.querySelector('.progress');const updateProgress=()=>{const h=document.documentElement;const sc=h.scrollTop/(h.scrollHeight-h.clientHeight);progress.style.width=(sc*100)+'%'};addEventListener('scroll',updateProgress,{passive:true});updateProgress();document.getElementById('year')&&(document.getElementById('year').textContent=new Date().getFullYear());const io=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')})},{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));const steps=[...document.querySelectorAll('.steps article')];const screen=document.getElementById('screenCard');const copy={Hook:['Step 1','Hook','Strong first impression and clear promise.'],Trust:['Step 2','Trust','Proof, pricing and useful explanations.'],Convert:['Step 3','Convert','Calls, WhatsApp and quote requests.'],'Follow up':['Step 4','Follow up','Automations keep leads moving.']};if(screen&&steps.length){const stepIo=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting){const c=copy[e.target.dataset.step]||copy.Hook;screen.innerHTML=`<span>${c[0]}</span><strong>${c[1]}</strong><p>${c[2]}</p>`;document.querySelector('.phone').style.transform=`rotate(${Math.random()>0.5?3:-4}deg) translateY(-8px)`;}})},{threshold:.55});steps.forEach(s=>stepIo.observe(s));}const canvas=document.getElementById('particles');if(canvas){const ctx=canvas.getContext('2d');let pts=[];function resize(){canvas.width=innerWidth*devicePixelRatio;canvas.height=innerHeight*devicePixelRatio;pts=Array.from({length:Math.min(90,Math.floor(innerWidth/16))},()=>({x:Math.random()*canvas.width,y:Math.random()*canvas.height,vx:(Math.random()-.5)*.35*devicePixelRatio,vy:(Math.random()-.5)*.35*devicePixelRatio,r:Math.random()*2+1}))}function tick(){ctx.clearRect(0,0,canvas.width,canvas.height);for(const p of pts){p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>canvas.width)p.vx*=-1;if(p.y<0||p.y>canvas.height)p.vy*=-1;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fillStyle='rgba(112,245,217,.45)';ctx.fill()}for(let i=0;i<pts.length;i++)for(let j=i+1;j<pts.length;j++){const a=pts[i],b=pts[j],d=Math.hypot(a.x-b.x,a.y-b.y);if(d<130*devicePixelRatio){ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.strokeStyle=`rgba(112,245,217,${(1-d/(130*devicePixelRatio))*0.18})`;ctx.stroke()}}requestAnimationFrame(tick)}resize();addEventListener('resize',resize);tick();}